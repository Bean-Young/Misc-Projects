# NNDL Class Model Weights

Original local folder: `NNDL-Class`.

This local folder contains trained neural network model checkpoints. The checkpoint files are not committed here because they exceed GitHub's normal single-file size limit.

## Local Files

| File | Approximate size |
| --- | ---: |
| `fcn_model_final.pth` | 612 MB |
| `resnet101_best_model.pth` | 173 MB |
| `resnet101_final_model.pth` | 173 MB |
| `resnet50_best_model.pth` | 100 MB |
| `resnet50_final_model.pth` | 100 MB |

Use Git LFS or an external release/storage service if these checkpoints need to be published later.
